import React from "react";
import {
  render
} from "react-dom";
import SearchParams from './SearchParams';

const App = () => {
  return ( <
    div >
    <
    h1 id = "something-important" > Adopt Me! < /h1> <
    SearchParams > < /SearchParams> <
    /div>
  );
};

CanvasRenderingContext2D( < App / > ), document.getElementById("root"));